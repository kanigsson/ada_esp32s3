name = "esp32s3_exceptions"
description = "ESP32-S3 demo of Ada exceptions: local, propagated, re-raised, and unhandled (last-chance handler)"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["embedded", "esp32s3", "exceptions", "baremetal", "example"]

project-files = ["app.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig       = { path = "../../crates/xtensa-dynconfig" }
