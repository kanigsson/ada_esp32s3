name = "esp32s3_ext4"
description = "Bare-metal Ada: mount a real ext4 SD card (pure-Ada filesystem over SD-over-SPI)"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["embedded", "esp32s3", "ext4", "filesystem", "sd", "example"]

project-files = ["ext4.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig       = { path = "../../crates/xtensa-dynconfig" }
