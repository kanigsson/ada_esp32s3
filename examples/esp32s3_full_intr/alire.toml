name = "esp32s3_full_intr"
description = "Full-profile interrupt-attach test (pragma Attach_Handler / Ada.Interrupts on ESP32S3_RTS_PROFILE=full)"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["embedded", "esp32s3", "example", "interrupt"]

project-files = ["example.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig = { path = "../../crates/xtensa-dynconfig" }
