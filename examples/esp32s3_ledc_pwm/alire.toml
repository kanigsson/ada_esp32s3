name = "esp32s3_ledc_pwm"
description = "Bare-metal Ada LEDC PWM self-test (GPIO-sampled) + RAII channel handle on the ESP32-S3 Jorvik runtime"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["embedded", "esp32s3", "ledc", "pwm", "driver", "example"]

project-files = ["ledc_pwm.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig       = { path = "../../crates/xtensa-dynconfig" }
