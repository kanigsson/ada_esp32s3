name = "esp32s3_rendezvous"
description = "ESP32-S3 demo of Ada task rendezvous (entries / accept / entry calls) on the full-tasking runtime"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["embedded", "esp32s3", "smp", "baremetal", "tasking", "rendezvous"]

project-files = ["app.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig       = { path = "../../crates/xtensa-dynconfig" }
