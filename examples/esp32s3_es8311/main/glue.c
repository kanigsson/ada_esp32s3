/* esp32s3_es8311: no example-specific C glue.
 *
 * All console output is formatted in Ada via ESP32S3.Log (the shared shim in
 * ../../common/bare/bare_log.c), so this file is intentionally empty.  The bare
 * build still compiles it -- it is the per-example glue slot.
 */
