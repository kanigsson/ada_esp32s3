name = "esp32s3_ext4_write"
description = "Bare-metal Ada: write + verify a file on an ext4 SD card over SDMMC (CH422G DAT3)"
version = "0.1.0-dev"

authors = ["D. Howells"]
maintainers = ["D. Howells <dhowells@motium.us>"]
licenses = "Apache-2.0 WITH LLVM-exception"
tags = ["embedded", "esp32s3", "ext4", "filesystem", "sdmmc", "write", "example"]

project-files = ["ext4_write.gpr"]

[[depends-on]]
esp32s3_rts = "*"

[[pins]]
esp32s3_rts = { path = "../../crates/esp32s3_rts" }
xtensa_dynconfig       = { path = "../../crates/xtensa-dynconfig" }
